function setup(canvas){const dpr=window.devicePixelRatio||1;const rect=canvas.getBoundingClientRect();canvas.width=Math.max(300,rect.width*dpr);canvas.height=(+canvas.getAttribute('height')||190)*dpr;const c=canvas.getContext('2d');c.scale(dpr,dpr);return {c,w:canvas.width/dpr,h:canvas.height/dpr};}
export function lineChart(canvas,points,{min=0,max=100,suffix=''}={}){const {c,w,h}=setup(canvas);c.clearRect(0,0,w,h);const pad={l:34,r:14,t:14,b:28};const iw=w-pad.l-pad.r,ih=h-pad.t-pad.b;c.strokeStyle='rgba(148,163,184,.22)';c.fillStyle='#94a3b8';c.font='11px system-ui';c.lineWidth=1;for(let i=0;i<=4;i++){const y=pad.t+ih*i/4;c.beginPath();c.moveTo(pad.l,y);c.lineTo(w-pad.r,y);c.stroke();const v=Math.round(max-(max-min)*i/4);c.fillText(v+suffix,2,y+4);}if(!points.length){c.fillStyle='#94a3b8';c.fillText('Sin datos suficientes',pad.l+10,h/2);return;}const x=i=>points.length===1?pad.l+iw/2:pad.l+iw*i/(points.length-1);const y=v=>pad.t+ih*(1-(v-min)/(max-min||1));c.strokeStyle='#38bdf8';c.lineWidth=3;c.beginPath();points.forEach((p,i)=>{const xx=x(i),yy=y(Math.max(min,Math.min(max,p.value)));i?c.lineTo(xx,yy):c.moveTo(xx,yy)});c.stroke();points.forEach((p,i)=>{const xx=x(i),yy=y(Math.max(min,Math.min(max,p.value)));c.fillStyle='#22c55e';c.beginPath();c.arc(xx,yy,4,0,Math.PI*2);c.fill();c.fillStyle='#94a3b8';const label=p.label||'';c.fillText(label,Math.max(0,xx-18),h-8)});}
